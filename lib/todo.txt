- CSV column layout/scroll
- Load total reimb value with transaction, and check this when adding a reimbursement for the target too
- Account card color bar
- Tabbing on forms
- Transaction screen filter values get deleted on tab switch and return, weird in filter grids too
- Filters for cash flow tab
- Category focus screen, show only category history respecting filters
- Hover on heatmap
- Double sided stack column chart
- Dark theme
- Replace all database calls with extension (see tags.dart).
- Replace income/expense categories in filters with a single "uncategorized"
- Get rid of DropdownButton and replace with newer MenuAnchor?
- Investment history bar chart; can just sum over investment account histories, no need for new category